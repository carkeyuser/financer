# Archiv

Archiv erledigter Feature-, Änderungs- und Bug-Backlog-Einträge. Offene Punkte bleiben in den jeweiligen Backlog-Dateien.

## Features

| # | Bereich | Beschreibung | Status |
|---|---|---|---|
| F-46 | DevOps / Einstellungen | **In-App-Update:** `/settings` UpdateCard; `GET /api/version`; `POST /api/admin/update` (NDJSON); `scripts/update.sh`; opt-in `docker-compose.update.yml` | ✅ erledigt 2026-06-04 |
| F-01 | Investments | Bearbeiten-Funktion: `PUT /api/assets/[id]` + `AssetEditDialog.tsx` | ✅ erledigt 2026-05-23 |
| F-02 | Haushaltskasse | Einnahmen pro User in `MonthlyEntryDialog` | ✅ bereits implementiert |
| F-03 | Investments | Historische Graphen: `getPortfolioValueHistory` fügt immer `today` hinzu | ✅ erledigt 2026-05-23 |
| F-04 | Investments | Drag & Drop + Card/List-Toggle in `InvestmentsContent`; `Asset.order`; `POST /api/assets/reorder` | ✅ erledigt 2026-05-23 |
| F-05 | Haushaltskasse | Fixkosten als Akkordeon in `HouseholdFinanceTable` | ✅ erledigt 2026-05-23 |
| F-06 | Auth | Username-only Auth; `email` optional; `twoFactorSecret` Platzhalter | ✅ erledigt 2026-05-23 |
| F-07 | Benutzer | Sidebar → „Benutzer"; `POST /api/admin/users` (OWNER-only); User direkt anlegen | ✅ erledigt 2026-05-23 |
| F-08 | Auth | **2FA (TOTP):** QR-Code generieren → bestätigen → Login prüft TOTP-Code. Bibliothek: `@otplib/preset-default`. `twoFactorEnabled`-Feld migriert. Login zwei-stufig via `/api/auth/check-2fa`. Admin-Toggle in `/household`. Setup in `/settings`. | ✅ erledigt 2026-05-23 |
| F-09 | Investments | Krypto-Suche: CoinGecko parallel zu Yahoo; BTC-USD-Format | ✅ erledigt 2026-05-23 |
| F-10 | Investments | **Familienweite Investments + Kontonamen:** `account`-Pflichtfeld; `@@unique([householdId, userId, ticker])`; alle Assets des Haushalts sichtbar; AssetCard zeigt Konto + Besitzer | ✅ erledigt 2026-05-23 |
| F-11 | Dashboard | **Widget-System:** `react-grid-layout`; 6 Widgets; Widget-Manager-Modal; DB-backed Layout; Marktkalender | ✅ erledigt 2026-05-23 |
| F-12 | Dashboard | **Auto-Sort-Button:** Button im Dashboard-Header der alle aktiven Widgets so anordnet, dass sie lückenlos auf die aktuelle Bildschirmbreite passen (kompaktes vertikales Packen, kein unnötiger Leerraum). | ✅ erledigt 2026-05-23 |
| F-13 | Dashboard | **Resize-Indikator:** In der unteren rechten Ecke jedes Widgets ein dezentes visuelles Element (z. B. Grip-Punkte), das signalisiert, dass die Größe veränderbar ist. | ✅ erledigt 2026-05-23 |
| F-14 | Investments | **Chart-Beschreibung (Tooltip):** In `PortfolioChartPanel` bei jedem Chart-Tab ein `i`-Icon — Hover zeigt Beschreibungstext. Umgesetzt mit shadcn Tooltip (@base-ui). | ✅ erledigt 2026-05-23 |
| F-15 | Navigation | **Sidebar-Klick-Animation:** `active:scale-95 transition-all duration-100` auf Nav-Links in `Sidebar.tsx`. | ✅ erledigt 2026-05-23 |
| F-16 | Settings | **2FA-Deaktivierung erfordert TOTP-Code:** `DELETE /api/user/2fa` prüft den Token; `SettingsContent` nutzt einen eigenen Bestätigungs-Step statt `confirm()`-Dialog. | ✅ erledigt 2026-05-23 |
| F-17 | Benutzer | **Admin: andere User bearbeiten:** In `/household` kann ein OWNER/ADMIN andere User per Bearbeitungs-Dialog umbenennen (Anzeigename + Loginname) und Passwort zurücksetzen. API: `PATCH /api/admin/users/[userId]`. | ✅ erledigt 2026-05-23 |
| F-18 | Investments | **Historischer Kaufpreis: G/V und Charts korrekt:** `getPortfolioValueHistory` und `getGainLossHistory` generieren monatliche Zwischenpunkte zwischen dem frühesten Entry-Datum und heute — Charts zeigen damit einen sinnvollen Verlauf auch ohne viele PRICE_UPDATE-Einträge. `getGainLossHistory` nutzt jetzt direkt die Datenpunkte von `getPortfolioValueHistory` (index-basierter Zugriff ersetzt). | ✅ erledigt 2026-05-23 |
| F-19 | Investments | **Sortierung nach Depot/Besitzer/Gesamtwert:** Sortier-Buttons (Depot alphabetisch, Besitzer, Wert ↓) in Investments-Übersicht. Aktive Sortierung wird durch DnD-Drag deaktiviert (manuelle Reihenfolge übernimmt). Standard-Ansicht: Liste. | ✅ erledigt 2026-05-23 |
| F-20 | Dashboard | **Neue Widgets:** `top-flop` (Top 3 / Flop 3 nach G/V%), `household-summary` (laufender Monat: Einnahmen, Fixkosten, freies Geld), `currency-exposure` (Währungsverteilung als Donut-Chart), `net-worth` (Portfoliowert + Jahresüberschuss aus Haushaltskasse). Alle 4 im Widget-Manager aktivierbar. | ✅ erledigt 2026-05-23 |
| F-21 | Investments | **Historische Yahoo-Finance-Kurse in Charts:** `GET /api/securities/history?symbol=&from=` ruft monatliche Schlusskurse von Yahoo Finance ab (v8/finance/chart, interval=1mo). `usePortfolioHistory`-Hook lädt die Daten für alle Assets parallel. `mergeHistoricalPrices()` in `calculations.ts` fügt synthetische PRICE_UPDATE-Einträge aus Yahoo-Kursdaten ein — echte manuelle Snapshots haben Vorrang. `PortfolioValueChart` und `PortfolioGainLossChart` nutzen den Hook: Charts zeigen nun den echten historischen Kursverlauf ab dem ersten Kaufdatum, auch wenn der Kauf Jahre in der Vergangenheit lag. EUR-Konvertierung nutzt den aktuellen EUR-Rate (akzeptable Näherung). | ✅ erledigt 2026-05-24 |
| F-22 | App | **Englischer Sprachmodus:** Umschaltung zwischen Deutsch und Englisch in den Einstellungen (`/settings`). Sprache wird pro User in der DB gespeichert (`User.locale: String @default("de")`). Alle UI-Texte, Labels, Fehlermeldungen, Chart-Achsen, Badge-Texte und Toast-Nachrichten werden übersetzt. Leichtgewichtiger React-Context + de/en-Messages (kein next-intl). Sidebar-Einträge, Monatsnamen, Zahlenformat (`de-DE` / `en-US`) und Währungsdarstellung passen sich an. | ✅ erledigt 2026-05-25 |
| F-23 | Investments | **Kurs per Klick aktualisieren (RefreshCw-Button):** RefreshCw-Button in der Button-Reihe der Asset-Detail-Seite. Ruft `GET /api/securities/price?symbol=` ab → wenn neuester Eintrag = `PRICE_UPDATE` von heute: PUT; sonst POST neuer `PRICE_UPDATE`-Eintrag. Button zeigt Lade-Spinner. | ✅ erledigt 2026-05-24 |
| F-24 | Investments | **Inline-Edit auf Asset-Detail-KPI-Cards:** Pencil-Icon auf drei Cards — Aktueller Wert, Menge, Ø Kaufpreis. Eingabe direkt in der Card (Inline-Input + OK/✕); Enter bestätigt, Escape bricht ab. Erstellt je einen Korrektureintrag: Wert → `PRICE_UPDATE` (Preis = EUR-Wert ÷ Menge ÷ eurRate); Menge → `QUANTITY_UPDATE` (setzt `asset.quantity` absolut); VWAP → `VWAP_UPDATE` (setzt Kostenbasis als price×qty). Neue DB-Enum-Werte: `QUANTITY_UPDATE`, `VWAP_UPDATE` (via `prisma db push` hinzugefügt). `getVWAP()` und `getGainLossHistory()` verarbeiten `VWAP_UPDATE` chronologisch. | ✅ erledigt 2026-05-24 |
| F-25 | Investments | **Filter + Sortierung nach Depot/Konto:** In der Investments-Übersicht (`InvestmentsContent`) wird die bestehende Sortier-Zeile (Depot/Besitzer/Wert-Buttons) um ein Filter-Dropdown erweitert — in derselben Zeile. Das Dropdown zeigt alle im Haushalt vorhandenen Depot-/Kontonamen (`asset.account`) sowie „Alle" als Standard. Bei aktivem Filter werden nur Assets des gewählten Kontos angezeigt; DnD-Drag und Sortier-Buttons bleiben unverändert nutzbar. Aktiver Filter bleibt bis zum Seitenwechsel erhalten (React-State, kein DB-Backing nötig). | ✅ erledigt 2026-05-25 |
| F-26 | Investments | **Logo / Typ-Badge pro Position:** In der Positionsübersicht (Card- und Listenansicht) wird ganz links vor dem Namen das Firmen-/Fondslogo angezeigt. Logo-Quelle: `https://logo.clearbit.com/<domain>` oder alternativ ein Finance-Logo-CDN (z. B. `https://financialmodelingprep.com/image-stock/<TICKER>.png`). Schlägt das Laden fehl (404, kein Logo vorhanden), wird stattdessen ein kompaktes Typ-Badge angezeigt (`ETF`, `KRYPTO`, `AKTIE`, `ANLEIHE`, `SONSTIGES`) — gleiche Darstellung wie der bestehende `AssetType`-Badge, aber als Fallback-Platzhalter mit Icon. | ✅ erledigt 2026-05-25 |
| F-27 | Investments | **Positions-Glow nach G/V:** Jede Asset-Card und Listenzeile bekommt einen subtilen farbigen Hintergrundschimmer: grün bei positivem G/V, rot bei negativem G/V, kein Glow (neutraler Hintergrund, `bg-card` — weiß/schwarz je nach Theme) wenn noch kein Kurs vorhanden oder G/V = 0 und keine Daten. Umsetzung via Tailwind-Klassen (`shadow-green-500/20`, `shadow-red-500/20` oder `ring`/`border`-Varianten) — kein harter Farbblock, nur ein dezenter Schimmer. | ✅ erledigt 2026-05-25 |
| F-28 | App | **Mobile Ansicht (Browser-Optimierung):** Responsive Anpassung der gesamten App für Smartphone-Browser. Smartphone bekommt eine mobile Topbar mit Hamburger-Menü statt Bottom-Navigation, Dashboard rendert Widgets auf Mobile als statische Liste ohne Drag/Resize-Griffe, Widgets können mobil per Hoch/Runter-Buttons verschoben und gespeichert werden, zentrale Tabellen wurden in Card-Ansichten übersetzt (Haushaltskasse, Investment-Einträge, Mitglieder), Formulare/Dialoge stapeln Felder auf kleinen Screens, Touch-Ziele sind mobil größer und ab `sm` wieder kompakt. | ✅ erledigt 2026-05-25 |
| F-29 | Investments | **Automatischer Kurs-Refresh (2h):** `POST /api/assets/refresh-prices` holt Yahoo-Kurse für alle Haushalts-Assets und upsertet `PRICE_UPDATE` für heute (`security-price.ts`). Client: `usePortfolioPriceRefresh` mit `refetchInterval` 2h + `refetchIntervalInBackground`; `PortfolioPriceRefresh` in `AuthGuard`. `usePortfolioHistory`: gleiches Intervall für Chart-Historie. Konstante: `PRICE_REFRESH_INTERVAL_MS`. | ✅ erledigt 2026-05-25 |
| F-32 | Investments | **Dividenden-Tab:** `/dividenden` erfasst Dividenden manuell auf Basis bestehender Positionen. Pflichtfelder sind Position und Gesamtsumme in EUR; Datum, Brutto, Steuer, Dividende/Aktie, Menge und Notiz sind optional. API: `/api/dividends/summary`, `/api/dividends/payments`. Dashboard-Widget `dividend-summary`, i18n de/en, Mobile-Ansicht und Unit-Tests. Ursprüngliche Yahoo-/Forecast-/Rechner-Logik wurde mit Ä-09 entfernt. | ✅ erledigt 2026-05-26 |
| F-35 | Dividenden | **Bankzinsen als Dividenden-Position `Interest`:** Pro User auto-provisioniertes Platzhalter-Asset (Ticker `Interest`), nur im Dividenden-Flow sichtbar — nicht in Investments, Dashboard-Portfolio oder Marktkalender. Buchung wie Dividende (Betrag + optional Datum/Steuer). | ✅ erledigt 2026-05-31 |
| F-33 | Haushaltskasse | **Simulation ab Startmonat:** Gespeicherte Szenarien mit frei wählbarem Start-/Endmonat. Baseline aus echten Monatsdaten/Snapshots, danach hypothetische Einnahmen, Fixkosten und Auszahlungen je Monat; optionale Übernahme ab einem Monat auf Folgemonate; Vorschau für Restgeld, theoretische/tatsächliche Auszahlung und Quartalsbonus. | ✅ erledigt 2026-05-25 |
| F-36 | Multi-Tenant / Benutzer | **Tenant-Provisioning (Hybrid):** Owner legt User mit `tenancy: tenant` an → neuer `Household`, User als OWNER, Standard-Fixkosten; kein Eintrag im Admin-Haushalt. `tenancy: household` unverändert (Mitglied im aktiven Haushalt). Isolation über `householdId` (JWT); gemeinsames Portfolio bleibt in Mehrpersonen-Haushalten. API `POST /api/admin/users`, UI Radio auf `/household`, Konstante `default-fixed-costs.ts`. | ✅ erledigt 2026-05-31 |
| F-36b | Dashboard | **Login Portfolio-Snapshot:** Nach jedem Login ein Dialog mit kombiniertem unrealisiertem G/V (EUR + %) der eigenen Positionen, Gesamtwert, Link zum Portfolio. `sessionStorage`-Flag; API `GET /api/dashboard/summary?scope=mine`; kein Popup bei leerem Portfolio oder FX-Fehler; nicht bei pending 2FA-Setup. | ✅ erledigt 2026-05-31 |
| F-37 | App | **Update-Dialog (What's New):** Nach Deploy einmalig Dialog mit kuratierten Release-Notes (de/en) aus `src/data/release-notes.ts`; Version-Vergleich via `localStorage`; Erstbesuch ohne Dialog; sequenziell vor Login-Snapshot; Settings zeigt Version + manuelles Öffnen. | ✅ erledigt 2026-05-31 |
| F-38 | Investments | **Trade-Republic-CSV-Import (Wizard):** Offizieller TR-Transaktionsexport → 7-Schritte-Wizard (Intro, Upload, Übersicht, Konflikte, Ticker, Bestätigen, Ergebnis). Hard-Dedup via `importRef` (Order ID); Soft-Match gegen manuelle Buchungen; Konflikt-Aktionen skip/import/link/replace. API `preview` + `apply`; ISIN→Ticker via Yahoo; Zinsen → Interest-Asset. Migration `importRef` auf `AssetEntry` + `DividendPayment`. | ✅ erledigt 2026-05-31 |
| F-38b | Investments | **Depot leeren vor TR-Import:** `DELETE /api/investments/accounts`; Button + Checkbox im Import-Wizard; löscht alle Positionen eines Depot-Namens inkl. Buchungen/Dividenden (Interest ausgenommen) für sauberen Neuimport. | ✅ erledigt 2026-06-01 |
| F-39 | Investments | **Positionen zusammenführen + Null-Filter:** Merge-Wizard mit ISIN-/Ticker-Vorschlägen und manueller Zuordnung; API `GET merge-suggestions`, `POST merge`, `POST merge/batch`; verschiebt Einträge + Dividenden; Filter „Leere ausblenden“ (default an); TR-Import ISIN-Lookup + Merge-CTA. | ✅ erledigt 2026-06-01 |
| F-40 | Dashboard | **Heute-Briefing:** `/heute`, `GET /api/today`, Portfolio/Top-Flop/Kalender/Haushalt/Dividenden/Checklist; „seit letztem Besuch“ via localStorage | ✅ erledigt 2026-06-01 |
| F-41 | Dashboard | **Vermögens-Snapshot:** `PortfolioDailySnapshot`, `GET /api/portfolio/snapshots`, Widget `portfolio-snapshot`, Upsert bei `refresh-prices` | ✅ erledigt 2026-06-01 |
| F-42 | App | **Notification Bell:** Glocke Sidebar/Mobile, `Notification` + `NotificationRead`, Generator (Kurs, Kalender, Haushalt, Quartal, Dividende, Partner), APIs | ✅ erledigt 2026-06-01 |
| F-43 | Haushaltskasse | **Monatsroutine:** `HouseholdMonthChecklist`, UI auf `/haushaltskasse` + `/heute`, `GET/PUT /api/household-finance/checklist` | ✅ erledigt 2026-06-01 |
| F-44 | Dashboard | **Dein Kalender:** Marktkalender filtert Depot + `?days=`, Hinweis im Widget, Briefing nutzt 7-Tage-Filter | ✅ erledigt 2026-06-01 |
| F-30 | DevOps | **GitHub-ready für Push:** `.gitignore` (Secrets, persönliche Deploy-Skripte, IDE-Config); IPs/Usernames anonymisiert; `push.example.ps1`/`pack.example.ps1`; MIT LICENSE; GitHub Actions CI; Initial Commit. | ✅ erledigt 2026-05-31 |

## Änderungen

| # | Bereich | Beschreibung | Status |
|---|---|---|---|
| Ä-01 | Investments | **Allokations-Chart Standard:** Standard-Ansicht auf „Nach Position" ändern (statt „Nach Typ"). `PortfolioAllocationChart.tsx`: `useState<"type" \| "position">("position")`. | ✅ erledigt 2026-05-23 |
| Ä-02 | Haushaltskasse | **Überschuss-Anzeige:** Einzelne monatliche Überschüsse entfernen; stattdessen einen einzigen Gesamtüberschuss ganz unten in der Tabelle / am Ende der Seite anzeigen. | ✅ erledigt 2026-05-23 |
| Ä-03 | Dashboard | **Widget-X-Button + Auto-Sort:** `WidgetShell` hat `onRemove`-Prop mit `×`-Icon (plain `<div>`-Header, kein `CardHeader`); `handleToggleWidget` ruft nach Entfernen automatisch `autoSortLayout()` auf. | ✅ erledigt 2026-05-23 |
| Ä-04 | Investments | **Listenansicht: Konto + Besitzer:** In der Listen-Ansicht zeigt die Position-Zelle jetzt `Landmark`-Icon + `account` und `User`-Icon + `ownerName` — analog zur Card-Ansicht. | ✅ erledigt 2026-05-23 |
| Ä-05 | Haushaltskasse | **Fixkosten prominenter darstellen:** Der Fixkosten-Bereich in `HouseholdFinanceTable` wurde deutlich sichtbarer gestaltet. | ✅ erledigt 2026-05-25 |
| Ä-06 | Dashboard | **Widget „Nettovermögen" → „Vermögen" ohne Jahresüberschuss:** Das Widget mit `widgetId: "net-worth"` wurde in „Vermögen" umbenannt; der angezeigte Wert ist nur noch der aktuelle Portfoliowert in EUR. | ✅ erledigt 2026-05-25 |
| Ä-07 | Haushaltskasse | **Simulation: Haken rechts farblich korrigieren:** Die Haken rechts in der Simulation wurden von gelb auf grün angepasst. | ✅ erledigt 2026-05-25 |
| Ä-08 | Haushaltskasse | **Monatliche Fixkosten einklappbar machen:** Die monatlichen Fixkosten in der Haushaltskassen-Ansicht sind ein- und ausklappbar, damit die Jahrestabelle kompakter bleibt. | ✅ erledigt 2026-05-25 |
| Ä-11 | Dokumentation | **Teststand konsolidiert:** Plan-Doku vereinheitlicht auf **97 Unit-Tests** (8 Dateien); `plan/setup.md` enthält vollständige Test-Matrix. | ✅ erledigt 2026-05-27 |
| Ä-11 | UI / Sidebar | **Changelog-Button in der Sidebar:** Button über Abmelden in Sidebar und Mobile-Menü; öffnet `UpdateNotesDialog` mit kuratierten Release-Notes aus `release-notes.ts`. | ✅ erledigt 2026-05-31 |
| Ä-10 | DevOps | **Lint für Next.js 16:** Migration `next lint` → ESLint 9 flat config (`eslint.config.mjs`, `eslint-config-next@16.2.6`); Ignore `src/generated/**`; Dialog-/Hook-Anpassungen für React-Hooks-Regeln; CI `npm run lint` vor Tests | ✅ erledigt 2026-06-04 |
| Ä-12 | Investments | **TR-Import — Dividenden in der Vorschau** | ✅ erledigt 2026-06-03 |
| Ä-13 | Investments | **TR-Import — Zeitraum per Häkchen** | ✅ erledigt 2026-06-03 |

| Ä-09 | Dividenden | **Dividendenbereich komplett manuell neu gebaut:** `/dividenden` ist jetzt eine einfache manuelle Buchungsliste mit Position + Gesamtsumme in EUR als Pflichtfelder. Datum, Brutto, Steuer, Dividende/Aktie, Menge und Notiz sind optional; fehlendes Datum wird als heute gespeichert. Yahoo-Dividendenservice, Ex-Date-Schätzung, Forward-12M, Forecast und Rechner wurden entfernt. Nacharbeit: `push.ps1` bereinigt `/opt/financer` vor dem Kopieren, damit entfernte Dateien nicht auf dem Server liegen bleiben. Verifiziert mit `npm run test` (80 grün) und `npm run build` (grün). | ✅ erledigt 2026-05-26 |

## Bugs

| # | Bereich | Beschreibung | Status |
|---|---|---|---|
| B-01 | Investments | Allokations-Chart: kleine Segmente (< 5%) → kein Label; `REAL_ESTATE`/`CASH` ergänzt | ✅ behoben 2026-05-23 |
| B-02 | Haushaltskasse | Fixkosten-Snapshot: nachträgliche Änderungen wirken nicht mehr auf abgeschlossene Monate zurück | ✅ behoben 2026-05-23 |
| B-03 | Dashboard | **Widget-Layout geht beim Refresh verloren:** `onSuccess: invalidateQueries` in `useSaveWidgets` entfernt; `useEffect` in `DashboardContent` lädt DB-Layout nur einmal (ref-Flag). | ✅ behoben 2026-05-23 |
| B-04 | Dashboard | **Marktkalender zeigt keine Daten:** `MarketCalendarWidget` bleibt leer, obwohl Investments eingetragen sind. Vermutlich schlägt der Yahoo-Finance-`calendarEvents`-Abruf in `/api/dashboard/market-calendar/route.ts` fehl (falsches Modul-Format, leeres Response, oder fehlende Ticker-Abfrage). | ✅ behoben 2026-05-23 |
| B-05 | Investments | **Positionen löschen: 403-Fehler:** `DELETE /api/assets/[id]` nutzte `requireHouseholdAdmin` — auf `requireSession` geändert; jedes Haushaltsmitglied kann Positionen löschen. | ✅ behoben 2026-05-23 |
| B-06 | Dashboard | **Widget-Layout wird nach Refresh nicht korrekt wiederhergestellt:** `onLayoutChange` speichert sofort; `ignoreLayoutChange`-Ref verhindert Save beim initialen DB-Load (100ms-Fenster). `handleToggleWidget` und `handleAutoSort` speichern ebenfalls direkt. | ✅ behoben 2026-05-23 |
| B-07 | Auth | **Abmelden → 404:** `signOut({ callbackUrl: "/auth/login" })` in `Sidebar.tsx` — bereits korrekt gesetzt. | ✅ behoben 2026-05-23 |
| B-08 | Auth | **IP-Direktaufruf → 404:** `src/app/login/page.tsx` leitet von `/login` → `/auth/login` weiter. | ✅ behoben 2026-05-23 |
| B-09 | Dashboard | **Widget-Layout wird beim ersten Mount mit DEFAULT_LAYOUT überschrieben:** `ignoreLayoutChange` war initial `false`. Beim ersten Mount feuert `react-grid-layout` sofort `onLayoutChange(DEFAULT_LAYOUT)` — noch bevor der `useEffect` das DB-Layout gesetzt hat. Fix: `ignoreLayoutChange = useRef(true)` damit dieser erste Call ignoriert wird. | ✅ behoben 2026-05-23 |
| B-10 | Dashboard | **Widget-Layout geht nach F5-Refresh verloren — vollständige Diagnose und Fix (supersedes B-03, B-06, B-09):** Das Problem hatte vier unabhängige Ursachen, die alle behoben werden mussten: **(1) Saves in `onLayoutChange` unzuverlässig:** Der `ignoreLayoutChange`-Ref mit 100ms-Timeout war eine Race-Condition — wenn das DB-Layout langsamer als 100ms ankam oder der Nutzer schnell agierte, wurden entweder falsche Saves ausgelöst oder Saves unterdrückt. Fix: Saves vollständig aus `onLayoutChange` heraus in `onDragStop`/`onResizeStop` verschoben — diese feuern exakt einmal nach abgeschlossener Nutzer-Interaktion. `onLayoutChange` macht nur noch `setLayout`. **(2) TanStack-Query-Cache nach SPA-Navigation veraltet:** Server-Logs zeigten: nach SPA-Navigation zurück zum Dashboard feuerte kein `GET /api/dashboard/widgets` — der Cache enthielt die Vor-Edit-Version. Fix: `staleTime: Infinity` (kein automatischer Hintergrund-Refetch) + `setQueryData` in `useSaveWidgets.onSuccess` (Cache nach jedem Save synchron aktualisieren). **(3) `useState(DEFAULT_LAYOUT)` überschrieb den korrekten Cache-Wert beim Re-Mount:** Auch wenn der Cache korrekt war, startete `useState` immer mit `DEFAULT_LAYOUT`. Fix: Lazy initializer `useState(() => savedLayout ?? DEFAULT_LAYOUT)` — liest den Cache-Wert beim ersten Mount; bei SPA-Navigation ist `savedLayout` sofort verfügbar. **(4) Flash von `DEFAULT_LAYOUT` nach F5:** Nach echtem Browser-Refresh ist der In-Memory-Cache leer → `useWidgets` gibt `undefined` zurück → `useState(() => undefined ?? DEFAULT_LAYOUT)` initialisiert mit `DEFAULT_LAYOUT` → `widgetsLoading` wird `false` → GridLayout rendert einmal mit `DEFAULT_LAYOUT` → `useEffect` korrigiert auf `savedLayout` → der kurze Flash sah für den Nutzer aus wie „Änderungen verloren und wieder hergestellt". Fix: Neuer State `layoutReady` (initialisiert als `() => !!savedLayout`); der `useEffect` setzt `setLayout(savedLayout)` + `setLayoutReady(true)` im selben React-Batch; `if (widgetsLoading \|\| !layoutReady)` gibt Lade-Spinner zurück — GridLayout wird erst gerendert, wenn das Layout sicher aus der DB stammt. Geänderte Dateien: `src/hooks/useWidgets.ts` (`staleTime`, `useQueryClient`, `setQueryData`), `src/app/dashboard/DashboardContent.tsx` (`onDragStop`/`onResizeStop`, lazy initializer, `layoutReady`). | ✅ behoben 2026-05-23 |
| B-11 | Settings | **Profil/Passwort-Änderung funktioniert nicht:** Vier Ursachen: (1) Session-Race — `useForm defaultValues` wurde vor dem async Session-Load gesetzt → Form startete leer; Fix: `useEffect` + `reset()` bei `session?.user?.id`-Änderung. (2) `username` fehlte in JWT/Session — `authorize()` gab kein `username` zurück, JWT-Callback speicherte es nicht, Session-Callback setzte es nicht; Fix: alle drei Stellen ergänzt + `trigger=update` behandelt `name` und `username`. (3) "Benutzername"-Feld war disabled und zeigte `session.user.name` statt echtem Username; Fix: editierbares Feld mit `profileSchema.username` (regex-validiert). (4) Passwortformular zeigte keine RHF-Validierungsfehler → Submit schien zu hängen; Fix: Fehlertext unter jedem Feld ergänzt. Geänderte Dateien: `src/types/next-auth.d.ts`, `src/lib/auth.ts`, `src/lib/validations/settings.ts`, `src/app/api/user/profile/route.ts`, `src/app/settings/SettingsContent.tsx`. | ✅ behoben 2026-05-23 |
| B-12 | Investments | **G/V-je-Position-Chart zeigt nicht alle Positionen:** `AssetGainLossBarChart` filtert mit `.filter((d) => d.gainLoss !== 0)` — Positionen ohne `PRICE_UPDATE` (letzter bekannter Preis = Kaufpreis) haben G/V = 0 und werden ausgeblendet. Fix: Filter entfernen, alle Positionen anzeigen. | ✅ behoben 2026-05-23 |
| B-13 | Settings | **Passwort ändern funktioniert nicht:** API und Logik sind korrekt; Bug war fehlende sichtbare Fehleranzeige (leere Felder lösten RHF-Validierung aus, kein Feedback). Fix: Inline-Fehler per `passwordForm.setError` statt nur Toast; API-Fehlermeldungen werden direkt unter dem Feld angezeigt. | ✅ behoben 2026-05-23 |
| B-14 | Investments | **Aktueller Wert zeigt USD-Betrag mit €-Zeichen:** `handleRefreshPrice` speicherte immer `data.price` (Nativwährung, z.B. USD), auch für Assets mit `currency = "EUR"`. Da `eurRate = 1` für EUR-Assets, wurde der USD-Betrag direkt als € angezeigt. Fix: `handleRefreshPrice` nutzt jetzt `priceEur` für EUR-Assets, `price` (nativ) für andere. Gleicher Fix in `AssetEntryForm`: Auto-Fill nutzt ebenfalls die Asset-Währung (`assetCurrency`-Prop, übergeben von `entry/page.tsx`). | ✅ behoben 2026-05-24 |
| B-15 | Investments | **Tortendiagramm: Beschriftungen gehen aus dem Fenster heraus:** In `PortfolioAllocationChart.tsx` rendern die `<Label>`-Elemente des `PieChart` bei vielen oder kleinen Segmenten außerhalb des SVG-Viewports. Fix: Segmente &lt; 10 % ohne Label (nur Tooltip mit %); 10–25 % Prozent innen im Segment; ≥ 25 % Name+% außen; `outerRadius` reduziert, Chart-Margin, `labelLine={false}`, lange Namen gekürzt. | ✅ behoben 2026-05-25 |
| B-16 | Deploy / Build | **Docker-Build scheitert (TypeScript):** `npm run build` / `docker compose up -d --build` bricht ab in `PortfolioAllocationChart.tsx` — `payload` existiert nicht auf `TooltipProps` (Recharts 3.x). Custom-Tooltip-Content braucht `TooltipContentProps` statt `TooltipProps` (Import + Signatur `AllocationTooltip`). | ✅ behoben 2026-05-25 |
| B-17 | Deploy | **`push.ps1` kopiert Artefakte mit:** Beim Deploy werden u. a. `.codegraph/codegraph.db*` (~6 MB) und `tsconfig.tsbuildinfo` per scp übertragen. Fix: in `push.ps1` robocopy `/XD ".codegraph"`, `/XF "*.tsbuildinfo"`; optional `.codegraph/` in `.gitignore`. Blockiert den Build nicht. | ✅ behoben 2026-05-25 |
| B-18 | Dashboard | **Marktkalender-Widget neu eingeordnet:** Dividenden sind nicht mehr vom alten `market-calendar`-Widget abhängig. F-32 liefert einen eigenen Dividenden-Tab plus `dividend-summary`-Dashboard-Widget; `market-calendar` kann als schlanker Earnings-/Termin-Block bestehen bleiben. | ✅ erledigt 2026-05-25 |
| B-19 | Dashboard | **`src/app/dashboard` resized nicht automatisch:** Das Dashboard nutzte eine eigene Breitenmessung mit initialem `window.innerWidth`, wodurch das Grid bei Fenstergrößenänderungen nicht zuverlässig auf die tatsächliche Containerbreite reagierte. Fix: `DashboardContent` nutzt jetzt `useContainerWidth()` aus `react-grid-layout`, rendert das Grid erst nach echter Messung und verwendet die v2-Props `gridConfig`/`dragConfig`; reine Resize-Events speichern weiterhin nichts in die DB. | ✅ behoben 2026-05-26 |
| B-08 | Auth / 2FA | **Admin-2FA-Toggle erzwingt Setup:** Admin-Aktivierung setzt nun einen Pending-Zustand ohne Secret. Passwort-Login leitet betroffene User nach `/settings?setup2fa=1`; `AuthGuard` blockt andere geschützte Bereiche bis zur TOTP-Bestätigung. Secrets werden erst nach gültigem Code gespeichert, damit unbestätigte Secrets nicht aktivierbar sind. | ✅ behoben 2026-05-26 |
| B-06 | Backup | **Restore lehnt aktuelle Backups ab:** `backupSchema` erlaubt nun auch `QUANTITY_UPDATE` und `VWAP_UPDATE`; das Schema liegt testbar unter `src/lib/validations/backup.ts`. | ✅ behoben 2026-05-27 |
| B-10 | Investments | **Aktueller Kurs kann veraltet sein:** `getCurrentPrice()` nutzt jetzt den zeitlich neuesten kursrelevanten Eintrag (`PRICE_UPDATE` oder `PURCHASE`) statt pauschal `PRICE_UPDATE` vor Käufen zu priorisieren. | ✅ behoben 2026-05-27 |
| B-11 | Haushaltskasse | **Monatseingaben können still fehlschlagen:** `useSaveMonthlyEntry()` prüft jetzt jede Income-/Payout-Response auf `res.ok` und wirft API-Fehler an den Dialog weiter; Teilfehler zeigen keinen Erfolgs-Toast mehr. | ✅ behoben 2026-05-27 |
| B-07 | Backup | **Backup/Restore vollständig:** Backups enthalten nun Dividenden und Haushaltskassen-Simulationen inklusive Monats-Entries; Restore löscht diese Haushaltsdaten vor dem Import vollständig und spielt sie neu ein. Alte Backup-Dateien ohne die neuen Felder bleiben importierbar. | ✅ behoben 2026-05-27 |
| B-12 | Investments / Währung | **FX-Ausfall verfälscht Werte:** `getEurRate()` fällt bei Yahoo-Fehlern für Fremdwährungen nicht mehr still auf `1.0` zurück. Ohne geladenen oder gecachten Wechselkurs geben Portfolio-/Dashboard-APIs 503 zurück; Kursdetails bleiben ohne EUR-Wert, statt native Preise als EUR zu speichern. | ✅ behoben 2026-05-27 |
| B-21 | Dashboard / Deploy | **UI eingefroren bei Marktkalender auf LXC:** Route `/api/dashboard/market-calendar` feuerte ~80+ parallele Nasdaq-Requests (60 Werktage); Timeouts blockierten den Node-Server → `failed to pipe response`, Klicks wirkten tot. Fix: `src/lib/services/nasdaq-calendar.ts` — 14 Werktage, max. 2 parallel, 8s Deadline, Fail-fast-Probe, `MARKET_CALENDAR_EXTERNAL=false`, Route liefert bei Fehler `[]`. | ✅ behoben 2026-05-27 |
| B-22 | TR-Import / Apply | **Fortschrittsbalken unvollständig:** Zins- und fehlende-Ticker-Zeilen riefen `onProgress` nicht auf. Fix: `finally`-Block in `applyTradeRepublicImport()` — jede Zeile meldet Fortschritt; Test `tr-import-apply-progress.test.ts`. | ✅ behoben 2026-05-31 |
| B-23 | TR-Import / Tickers | **Konflikt-Ticker blieb in „Zu prüfen“:** `tickerMappingNeedsAttention()` ignorierte gesetzten Override bei `hasTickerConflict`. Fix: mit Override → erledigt; Sortierung angepasst; Test in `tr-import-sort.test.ts`. | ✅ behoben 2026-05-31 |
| B-24 | TR-Import / UI | **ETA/Prozent erst ab 2. Event:** `startRef` in `useImportEta` synchron beim ersten Progress setzen. | ✅ behoben 2026-05-31 |
| B-25 | TR-Import / ISIN | **Parallele Yahoo-Auflösung:** Retry mit Backoff, Default-Concurrency 2, konfigurierbar. | ✅ behoben 2026-05-31 |
| B-26 | TR-Import / API | **Stream-Fehler HTTP 200:** `createNdjsonResponse` liefert vor erstem Progress JSON 500; Mid-Stream-Fehler weiter NDJSON `error`. | ✅ behoben 2026-05-31 |
| B-27 | TR-Import / Wizard | **Leerer Tickers-Step:** Navigation nur bei `tickerPartition.needsAttention.length > 0`. | ✅ behoben 2026-05-31 |
| B-28 | TR-Import / Stream | **Keine Validierung von `complete.data`:** `readNdjsonStream` validiert Events per Zod; optional `resultSchema` für Complete-Payload; Hook nutzt Preview-/Apply-Schemas. | ✅ behoben 2026-05-31 |
| B-29 | TR-Import / Stream | **NDJSON parse ohne Fehlerbehandlung:** `JSON.parse` in try/catch; ungültige Zeilen → klare Fehlermeldung. | ✅ behoben 2026-05-31 |
| B-30 | TR-Import / UI | **Toter Code `useImportProgressReader`:** Hook entfernt; Fortschritt weiter über `useTradeRepublicImport`. | ✅ behoben 2026-05-31 |
| B-31 | TR-Import / UI | **Balken bei `current: 0` leer:** `computeWeightedProgress` zeigt 5 % Phasenanteil bei Start; Panel mindestens 1 % Anzeige. | ✅ behoben 2026-05-31 |
| B-32 | TR-Import / Tests | **Fehlende Tests:** `tr-import-routes.test.ts` (Preview/Apply NDJSON); `isin-resolver.test.ts` (parallel + `onProgress`). | ✅ behoben 2026-05-31 |
| B-33 | TR-Import / Wizard | **Krypto ohne ISIN:** Ticker-Step übersprungen (nicht in `tickerMappings`); Bestätigung zählte `needs_ticker` fälschlich als Import; Apply brach bei erster Fehlerzeile ab. | ✅ behoben 2026-05-31 |
| B-34 | TR-Import / Parser | **Ungültige Menge oder Preis:** CSV mit `Amount`/`Betrag` ohne `Rate`/`Kurs`; Kurs wird aus Betrag÷Stück abgeleitet. | ✅ behoben 2026-05-31 |
| B-35 | F-39 / Merge | **VWAP_UPDATE ignoriert in Mengenberechnung:** `recalculateQuantityFromEntries` berücksichtigt VWAP_UPDATE; Merge-Apply übergibt `price`. | ✅ behoben 2026-06-01 |
| B-36 | F-39 / Matching | **Regel „eine ISIN + Name“ feuerte nie:** `matchAssetPair` prüft one-ISIN vor Ticker-Match. | ✅ behoben 2026-06-01 |
| B-37 | F-39 / Matching | **Transitive False-Positives:** Union-Find-Komponenten ≥3 werden per Clique-Split aufgeteilt. | ✅ behoben 2026-06-01 |
| B-38 | F-39 / TR-Import | **ISIN-Backfill fehlte:** `getOrCreateAsset` schreibt ISIN bei Ticker-Match nach. | ✅ behoben 2026-06-01 |
| B-39 | F-39 / Wizard | **Step-Regression nach Merge:** Scan per NDJSON-Stream; kein Query-Refetch überschreibt `result`-Step. | ✅ behoben 2026-06-01 |

| B-09 | Investments | **G/V-Historie nach Verkäufen/Mengenkorrekturen falsch:** `getGainLossHistory()` nutzt jetzt eine chronologische Positions-Replay-Logik, die `SALE`, `QUANTITY_UPDATE` und `VWAP_UPDATE` in Menge und Kostenbasis berücksichtigt. | ✅ behoben 2026-05-27 |
